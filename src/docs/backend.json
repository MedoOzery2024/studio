{
  "entities": {
    "UploadedFile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UploadedFile",
      "type": "object",
      "description": "Represents a file or text content generated and saved by a user. This can include images, PDFs, Word documents, PowerPoint presentations, and text documents, as well as transcribed and summarized text.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the uploaded file or content."
        },
        "fileName": {
          "type": "string",
          "description": "The name of the file or content."
        },
        "fileType": {
          "type": "string",
          "description": "The MIME type of the file (e.g., 'image/jpeg', 'application/pdf', 'text/plain')."
        },
        "fileSize": {
          "type": "number",
          "description": "The size of the file in bytes (if applicable)."
        },
        "uploadDate": {
          "type": "string",
          "description": "The date and time when the file was uploaded or content was created.",
          "format": "date-time"
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N UploadedFile). The user who created or uploaded the content."
        },
        "storageLocation": {
          "type": "string",
          "description": "The location where the file is stored (e.g., cloud storage URL). Not applicable for text content stored directly in Firestore."
        },
        "transcribedText": {
            "type": "string",
            "description": "The full text content, e.g., from speech-to-text."
        },
        "summarizedText": {
            "type": "string",
            "description": "The summarized version of the text content."
        }
      },
      "required": [
        "id",
        "fileName",
        "fileType",
        "uploadDate",
        "userId"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Medo.Ai Time Hub application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "userName": {
          "type": "string",
          "description": "The username of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "registrationDate": {
          "type": "string",
          "description": "The date and time when the user registered.",
          "format": "date-time"
        },
        "lastLogin": {
          "type": "string",
          "description": "The date and time of the user's last login.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userName",
        "email",
        "registrationDate"
      ]
    },
    "QuestionSession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "QuestionSession",
      "type": "object",
      "description": "Represents a saved session of generated questions.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the question session."
        },
        "fileName": {
          "type": "string",
          "description": "The name of the session or the source file name."
        },
        "questions": {
          "type": "array",
          "description": "An array of generated questions.",
          "items": {
            "type": "object",
            "properties": {
              "question": {"type": "string"},
              "options": {"type": "array", "items": {"type": "string"}},
              "correctAnswer": {"type": "string"},
              "explanation": {"type": "string"}
            },
            "required": ["question", "options", "correctAnswer", "explanation"]
          }
        },
        "isInteractive": {
          "type": "boolean",
          "description": "Whether the questions are multiple-choice."
        },
        "uploadDate": {
          "type": "string",
          "description": "The date and time when the session was saved.",
          "format": "date-time"
        },
        "userId": {
            "type": "string",
            "description": "The user who created the session."
        }
      },
      "required": ["id", "fileName", "questions", "isInteractive", "uploadDate", "userId"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores user profile information. Path-based ownership ensures only the user can modify their own profile."
    },
    "/users/{userId}/uploadedFiles/{fileId}": {
      "schema": {
        "$ref": "#/entities/UploadedFile"
      },
      "description": "Stores files and text content created by each user. Includes denormalized 'userId' for authorization independence."
    },
    "/users/{userId}/questionSessions/{sessionId}": {
      "schema": {
        "$ref": "#/entities/QuestionSession"
      },
      "description": "Stores generated question sessions for each user. Path-based ownership ensures only the user can access their own sessions."
    }
  }
}
